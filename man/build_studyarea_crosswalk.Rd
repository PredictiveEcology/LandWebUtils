% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/studyAreaGroups.R
\name{build_studyarea_crosswalk}
\alias{build_studyarea_crosswalk}
\title{Build the LandWeb study-area grouping crosswalk}
\usage{
build_studyarea_crosswalk(
  fmas,
  eco,
  eco_field = "REGION_NAM",
  res_m = 240,
  min_area_km2 = 100
)
}
\arguments{
\item{fmas}{\code{sf} of the combined LandWeb FMA boundaries (e.g. \code{\link[=prepFMAs]{prepFMAs()}}).}

\item{eco}{\code{sf} of ecological units to group by (e.g. an ecoregions layer). Supply
an ecoprovince or ecodistrict layer to change the grouping granularity.}

\item{eco_field}{character naming the column in \code{eco} that holds each unit's name
(default \code{"REGION_NAM"}, the ecoregion name field).}

\item{res_m}{numeric simulation pixel size in metres, used only for the
\code{mpix} size estimate (default \code{240}).}

\item{min_area_km2}{numeric; member tenures smaller than this are dropped as
sliver artifacts (with a message listing them), since they cannot form a
meaningful study area. Default \code{100}. Set to \code{0} to keep everything.}
}
\value{
A \code{data.frame} crosswalk, one row per member tenure, with columns:
\code{group} (study-area group token), \code{fma_name} (the member's coalesced
FMA/TSA/FML identity --- the join key back to \code{fmas}), \code{company} (licensee, for
company-driven reruns), \code{province}, \code{eco_unit} (the dominant ecological unit),
\code{area_km2}, and \code{mpix} (approx. pixels at \code{res_m}). Sort/split by \code{group} to get
each study area's member set.
}
\description{
Groups the LandWeb FMA/TSA/FML boundary polygons into ecologically-coherent
study areas by assigning each to its \strong{dominant ecological unit} (an ecoregion by
default) via spatial overlap, then names the group after that unit. Small,
scattered forest tenures (e.g. a single small FMA) are thereby run \emph{inside} a
larger, ecologically homogeneous area, so the downstream \code{studyAreaANPP} PSP
catchment is both large enough for trait estimation and ecologically appropriate
(species traits can differ across ecoregions, so PSP pools should not span
drastically different ecology).
}
\details{
The grouping unit is a parameter: pass a coarser layer (ecoprovinces) for fewer,
larger groups, or a finer one (ecodistricts) to subdivide groups that prove too
large computationally. Membership is by \strong{verified spatial overlap against the
current FMA layer} --- never by company/tenure labels, which drift over time (a
polygon labelled for one licensee in an older file may belong to another now).
}
