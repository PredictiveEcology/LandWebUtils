% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caribou.R
\name{buildCaribouRanges}
\alias{buildCaribouRanges}
\title{Assemble the caribou range reporting layer}
\usage{
buildCaribouRanges(
  studyArea,
  destinationPath,
  targetCRS = LandWebCRS,
  layers = caribouRangeLayers()
)
}
\arguments{
\item{studyArea}{A \code{SpatVector} (or anything \code{\link[terra:vect]{terra::vect()}} accepts).}

\item{destinationPath}{Directory for downloads/extraction.}

\item{targetCRS}{Target CRS (default \link{LandWebCRS}).}

\item{layers}{Candidate-source table (default \code{\link[=caribouRangeLayers]{caribouRangeLayers()}}).}
}
\value{
A \code{SpatVector} with columns \code{Name}, \code{juris} and \code{ecotype}, or \code{NULL} if no
source intersects \code{studyArea}.
}
\description{
Fetch each jurisdictional source in \code{\link[=caribouRangeLayers]{caribouRangeLayers()}}, drop locally extirpated
herds, label it from its \code{labelCols}, tag its \code{juris} and boreal/mountain \code{ecotype},
clip it to \code{studyArea} with \code{\link[spatialutils:prep_vector]{spatialutils::prep_vector()}}, and bind the intersecting
pieces into one layer. Sources that do not reach the study area are dropped, so this
works unchanged for a study area in any jurisdiction.
}
\details{
Range names are normalised via an explicit spelling table (see the source of
\code{\link[=caribouRangeLayers]{caribouRangeLayers()}}): a range that two provinces spell differently would otherwise
appear as two reporting units, since the summaries group by \strong{name} rather than by
feature. That same name-grouping is what stitches a cross-border range whose two
provincial shares carry the same name --- \code{Chinchaga} and \code{Narraway} --- back into a
single reporting unit.
}
\seealso{
\code{\link[=caribouRangeLayers]{caribouRangeLayers()}}, \code{\link[=buildReportingPolygons]{buildReportingPolygons()}}
}
