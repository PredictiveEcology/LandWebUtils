% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-events.R
\name{landmine_area_burned_by_zone}
\alias{landmine_area_burned_by_zone}
\title{Area burned per FRI zone in a single year}
\usage{
landmine_area_burned_by_zone(
  currentBurn,
  fireReturnInterval,
  time,
  pixelAreaHa
)
}
\arguments{
\item{currentBurn}{\code{SpatRaster} of this year's burn; burned pixels are \code{1}, unburned \code{0}.}

\item{fireReturnInterval}{\code{SpatRaster} of per-pixel FRI values (the zone labels).}

\item{time}{numeric, the simulation time recorded in the returned table.}

\item{pixelAreaHa}{numeric, the area of one pixel in hectares.}
}
\value{
A \code{data.frame} with columns \code{time}, \code{nPixelsBurned}, \code{haBurned}, \code{FRI} (a factor).
}
\description{
Counts burned pixels of \code{currentBurn} within each FRI zone and converts to hectares.
}
\details{
\strong{Fixes a bug.} This previously counted burned pixels with
\code{unname(table(currBurn[ids])[2])} -- positional indexing into a \code{table()}, which assumes
the levels are always \verb{\{0, 1\}} in that order. When a zone burns \strong{completely} there is
only one level, so \verb{[2]} is \code{NA}, and the following \code{npix[is.na(npix)] <- 0} (whose comment
says it handles the \emph{no-burn} case) silently converted it to its exact opposite: a
fully-burned zone was recorded as \strong{zero} hectares burned. Counting with
\code{sum(vals == 1, na.rm = TRUE)} is correct in all three cases.
}
