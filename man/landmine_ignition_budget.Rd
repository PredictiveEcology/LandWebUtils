% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-events.R
\name{landmine_ignition_budget}
\alias{landmine_ignition_budget}
\title{Ignition budget per FRI zone}
\usage{
landmine_ignition_budget(
  fireReturnInterval,
  flammableMap,
  kBest,
  biggestPossibleFireSizeHa
)
}
\arguments{
\item{fireReturnInterval}{\code{SpatRaster} whose pixel values are the target fire return
interval. Zero-valued and non-flammable pixels are set to \code{NA}.}

\item{flammableMap}{\code{SpatRaster}; pixels that are \code{NA} or \code{0} are non-flammable.}

\item{kBest}{numeric, the fitted truncated-Pareto shape for the fire-size distribution.}

\item{biggestPossibleFireSizeHa}{numeric, the distribution's upper bound, in hectares.}
}
\value{
A list with:
\describe{
\item{\code{fireReturnInterval}}{the masked raster.}
\item{\code{fireReturnIntervalsByPolygonNumeric}}{FRI per zone, ascending, with a trailing \code{NA}.}
\item{\code{numFiresPerYear}}{expected fires per year per zone, named by FRI, in the same order.}
}
}
\description{
Masks ineligible pixels out of the fire-return-interval raster, tabulates pixels per FRI
zone, and converts that to an expected number of fires per year per zone.
}
\details{
The budget is \verb{(zone area / mean fire size) / FRI}, so the expected area burned per year in a
zone equals \code{area / FRI} by construction -- provided each fire ignites in the zone, reaches
its drawn size, and burns inside the zone.

\strong{This function creates an ordering contract that the reburn loop then consumes
positionally.} Downstream, \code{numFiresThisPeriod[.GRP]} indexes the per-zone fire counts by
the \emph{group position} of a \code{polygonNumeric}-keyed table, which is valid only because:
\enumerate{
\item \code{terra::freq()} returns rows sorted ascending by value;
\item the \code{value = NA} row is appended \strong{last};
\item dividing by the \code{NA} return interval makes that entry's \strong{value} \code{NA}, so a later
\code{na.omit()} drops it -- an \code{NA} \emph{name} alone would not.
}

Break any one and zones are handed each other's fire counts, with no error. All three are
pinned by tests here, at the point where the contract is created.

Non-flammable pixels are masked out of the raster \emph{before} tabulation, so the budget is a
flammable-area budget and matches how achieved FRI is measured in
\code{\link[=landmine_fri_summary]{landmine_fri_summary()}}. It also means such pixels can never be ignition locations, since
the start-cell pool is built from this same masked raster.
}
