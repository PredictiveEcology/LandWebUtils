% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-events.R
\name{landmine_reburn_budget}
\alias{landmine_reburn_budget}
\title{Rebuild the per-zone fire budget for the next reburn round}
\usage{
landmine_reburn_budget(tooSmallByPoly, friByPolygon, remainingSize = NULL)
}
\arguments{
\item{tooSmallByPoly}{a \code{data.table} of the fires that did not reach their target, joined to
their ignition zone: needs \code{polygonNumeric}, \code{maxSize}, and (optionally) \code{pixel}.}

\item{friByPolygon}{the per-zone FRI vector from \code{\link[=landmine_ignition_budget]{landmine_ignition_budget()}} -- ascending,
with a trailing \code{NA}. Its order \strong{defines} the output order.}

\item{remainingSize}{optional numeric; phase 2 passes the \emph{remaining shortfall} per fire, which
replaces \code{maxSize}. Phase 1 leaves it \code{NULL}, keeping each fire's full original target.}
}
\value{
A list with \code{polysNeedMoreFires} (the joined table), \code{numFiresThisPeriod} (fires still
needed per zone, in \code{friByPolygon} order) and \code{fireSizesInPixels} (target sizes, positionally
paired with the start cells to be drawn).
}
\description{
After a burn round, decides how many fires each FRI zone still needs and at what target
sizes, for both reburn phases.
}
\details{
Three positional contracts live here, none of which errors when broken:
\itemize{
\item the right join is driven by \code{friByPolygon}, so the result -- and hence
\code{numFiresThisPeriod} -- is in \strong{that} order (zones ascending, \code{NA} row last). The caller
indexes it with \verb{[.GRP]} against a \code{polygonNumeric}-keyed table whose groups are also
ascending, and the trailing \code{NA} row is never reached because that table has no \code{NA} zone.
\item \code{na.omit()} on the whole table is load-bearing: it drops both the zero-shortfall zones
(\code{maxSize} is \code{NA} where the join found no match) and the \code{NA}-FRI row, leaving
\code{fireSizesInPixels} paired \strong{positionally} with the start cells the caller then draws
\code{by = polygonNumeric}. It would also silently drop a row if any other column went \code{NA}.
\item \code{remainingSize} is assigned \strong{by position} onto the too-small rows; both descend from the
same \code{fa[tooSmall]} subset, so they agree -- but nothing enforces it.
}

\code{remainingSize} can be \code{0} for a fire that burned exactly its target; the caller's
\code{fireSizesInPixels > 0} filter is what removes those.

Unlike the inline code this replaces, the input table is \strong{not} modified by reference.
}
