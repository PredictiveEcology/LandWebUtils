% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/current-conditions.R
\name{cc_age_composite}
\alias{cc_age_composite}
\title{Composite current-condition stand age, filling gaps from a second source}
\usage{
cc_age_composite(
  base,
  studyArea,
  fill = NULL,
  fillYear = NULL,
  epoch = 2025L,
  baseNAflag = 65535,
  fillNAflag = 255
)
}
\arguments{
\item{base}{\code{SpatRaster} of stand age at \code{epoch}; authoritative wherever it has a value.}

\item{studyArea}{\code{SpatVector} or \code{sf} polygon(s). Both rasters are cropped to it \strong{before} any
other work (see Details).}

\item{fill}{optional \code{SpatRaster} of stand age at \code{fillYear}, in any CRS.}

\item{fillYear}{the year \code{fill} represents. Required with \code{fill}.}

\item{epoch}{the year \code{base} represents; \code{fill} is aged forward by \code{epoch - fillYear}.}

\item{baseNAflag, fillNAflag}{the NoData sentinel of each source (\code{age_in2025}: 65535; NTEMS: 255,
meaning non-treed). \code{NA} if the source has none.}
}
\value{
A \code{SpatRaster} on \code{base}'s grid, cropped and masked to \code{studyArea}.
}
\description{
Crops the primary age raster to the study area, converts its NoData sentinel to \code{NA}, and
optionally fills the cells it leaves empty from a second age raster aged forward to the same
epoch. Promoted from \code{LandWeb_preamble}, where \code{base} is fRI Research's \code{age_in2025} and \code{fill}
is NTEMS forest age.
}
\section{Fill, never minimum}{
\code{base} stays authoritative: \code{fill} only supplies cells \code{base} left empty. A cell-wise minimum
would instead let a \emph{modelled} age drag \emph{inventory} ages younger, and because a minimum is
one-directional that is a systematic young bias into exactly the areas where the data is best.
}

\section{Sentinels}{
A sentinel left unconverted is read as a real age -- 65,535 years, or a 255-year stand on every
non-treed pixel -- and silently poisons every downstream aggregate. \code{terra} does not read
\code{age_in2025}'s NoData tag, so the sentinel must be supplied. It is converted with
\code{terra::classify()} after cropping rather than with \verb{NAflag<-}: for an in-memory raster,
\verb{NAflag<-} converts the values but \code{NAflag()} still reports \code{NaN}, so an assertion on the flag
fails on correct data, and whether the flag "took" depends on how the raster is stored.

\code{fill}'s sentinel is converted \strong{before} it is reprojected and aged, so it can never surface as
\code{sentinel + (epoch - fillYear)} (e.g. 258).
}

\section{Cost}{
Cropping first matters: \code{age_in2025} alone is about 6.5e9 cells, so aligning a fill at full
extent would move billions of cells to produce one study area's output. \code{fill} is reprojected
with \code{method = "near"}, so the result contains only the source's own ages.
}

\seealso{
\code{\link[=cc_age_pct_missing]{cc_age_pct_missing()}} to check the result is complete enough to use.
}
