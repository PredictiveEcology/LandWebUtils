% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-events.R
\name{landmine_draw_num_fires}
\alias{landmine_draw_num_fires}
\title{Draw the number of fires per FRI zone for one period}
\usage{
landmine_draw_num_fires(numFiresPerYear, fireTimestep = 1, size = 1.3765)
}
\arguments{
\item{numFiresPerYear}{numeric, expected fires per year for each zone.}

\item{fireTimestep}{numeric, years per burn event.}

\item{size}{numeric, the negative-binomial dispersion. The default of \code{1.3765} is
LandMine's calibrated value (lowered from \code{1.8765} in 2018 because the draw was
otherwise too constant); it is deliberately overdispersed, so a zone with a small
\code{numFiresPerYear} sees long runs of zero-fire years punctuated by bursts.}
}
\value{
An integer vector, one count per element of \code{numFiresPerYear}, with names preserved.
}
\description{
Draw the number of fires per FRI zone for one period
}
