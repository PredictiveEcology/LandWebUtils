% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-fri-diagnostics.R
\name{landmine_fri_metrics}
\alias{landmine_fri_metrics}
\title{Per-zone fire-return-interval diagnostics}
\usage{
landmine_fri_metrics(
  lthfc,
  flammableMap,
  meanAnnualCumulBurnMap,
  studyAreaName,
  pixelAreaHa,
  studyArea = NULL,
  vegTypeMap = NULL,
  vegTypeMapInit = NULL,
  lccMap = NULL,
  treeClasses = NULL,
  tolerance = c(0.8, 1.25),
  severe = c(0.5, 2),
  minIgnitions = 20L,
  nYears = NA_real_
)
}
\arguments{
\item{lthfc}{\code{SpatRaster} of target fire return intervals (the "LTHFC").}

\item{flammableMap}{\code{SpatRaster}; \code{1} is flammable, \code{0} and \code{NA} are not.}

\item{meanAnnualCumulBurnMap}{\code{SpatRaster} of mean annual burn counts per pixel.}

\item{studyAreaName}{character, recorded in the returned table.}

\item{pixelAreaHa}{area of one pixel, in hectares.}

\item{studyArea}{optional \code{SpatVector}/\code{sf} polygon of the area the fire model was
actually allowed to burn. \strong{Supplying this is strongly recommended}; see Details.}

\item{vegTypeMap}{optional \code{SpatRaster} of leading vegetation type. When supplied, a
\code{pctNoCohorts} column reports the share of each zone's flammable pixels that carry
no cohorts at all -- non-forest land, not young forest. See Details.}

\item{vegTypeMapInit}{optional \code{SpatRaster} of leading vegetation type at the START
of the run. Supplying it separates land that never had cohorts from land that lost
them, via \code{pctCohortsInit} and \code{pctCohortsLost}.}

\item{lccMap}{optional \code{SpatRaster} of land cover class, on the same grid as \code{lthfc}.}

\item{treeClasses}{integer vector of \code{lccMap} values that count as treed. Required
with \code{lccMap}.}

\item{tolerance}{length-2 numeric; achieved/target ratios outside this range are
flagged. Symmetric in log space by default.}

\item{severe}{length-2 numeric; ratios outside this range are flagged \code{"severe"}.}

\item{minIgnitions}{integer; a zone expected to see fewer ignitions than this over
the whole run is reported but not judged, because its achieved FRI is dominated
by sampling noise. Set \code{NA} to judge every zone.}

\item{nYears}{length of the run in years, used with \code{minIgnitions}. \code{NA} skips the
noise check.}
}
\value{
A \code{data.table}, one row per zone, sorted by \code{LTHFC}, with columns
\code{studyArea}, \code{LTHFC}, \code{FRI}, \code{ratio}, \code{status}, \code{areaHa}, \code{nTotal}, \code{nFlam},
\code{pctFlam}, \code{nPatches}, \code{largestPatchPct}, \code{pctNoCohorts}, \code{pctCohortsInit},
\code{pctCohortsLost}, \code{pctTreedLCC}, \code{cohortGap}, \code{pctInStudyArea}, \code{FRIunmasked},
\code{ratioUnmasked} and \code{expIgnitions}. The columns
depending on an optional input are \code{NA} when it is not supplied.
}
\description{
Extends \code{\link[=landmine_fri_summary]{landmine_fri_summary()}} with the structural measurements needed to tell
\emph{why} a fire-return-interval zone misses its target, and classifies each zone
against a tolerance.
}
\details{
\code{ratio} is \strong{achieved / target}, so a value above 1 means the zone burns \emph{less}
often than intended (under-burning) and below 1 means more often. That orientation
is what the diverging fill in \code{\link[=landmine_plot_fri_zones]{landmine_plot_fri_zones()}} is centred on.
}
\section{Why \code{studyArea} matters}{
LandMine masks both its rate-of-spread map and its spread probability to
\code{sim$studyArea}, so pixels outside that polygon cannot be ignited or spread into. The
fire-return-interval raster is not similarly masked, so it commonly extends past the
polygon -- and \code{\link[=landmine_fri_summary]{landmine_fri_summary()}} divides burns by \strong{every} flammable pixel
carrying an interval, unburnable ones included. Each such pixel inflates a zone's
achieved interval without contributing any opportunity to burn.

This is not a small correction. On WesternAlbertaUpland, 988,437 of 3,361,240
flammable zone pixels (29.4\%) lay outside the polygon and burned at a mean rate of
0.51 against 16.59 inside. Uncorrected, two zones appeared to under-burn by 3.5x and
12.2x; with the denominator restricted to the burnable area, every zone came in
between 0.90 and 1.07 -- there was no attainment problem at all. Supply \code{studyArea}
and both figures are reported, \code{ratio} from the burnable area and \code{ratioUnmasked}
the way the module's own summary computes it, so the discrepancy is visible rather
than mistaken for a fire-model failure.

The structural columns exist because the failure modes are not distinguishable from
the ratio alone:
\itemize{
\item \code{pctFlam} -- the fraction of the zone that can carry fire at all. \code{spread2()}
treats a non-flammable neighbour as a hard mask, so a porous zone loses perimeter
growth every iteration and its fires stall short of their target size. On
WesternAlbertaUpland the two failing zones were the only ones below 93\% flammable
(54.6\% and 57.2\%), which no previously-reported metric would have shown -- the
landscape-wide figure was 89\%.
\item \code{nPatches} / \code{largestPatchPct} -- these separate \emph{porous} from \emph{fragmented}. A zone
can be half non-flammable and still be one connected sheet (zone 170's largest
patch held 86.8\% of its flammable pixels), which is a very different problem from
a zone broken into islands smaller than the fire-size draw.
\item \code{pctNoCohorts} -- on WesternAlbertaUpland this tracked the shortfall more closely
than anything else: every zone below 5\% hit its target (ratios 0.96-1.00), while
the two worst zones were at 87.0\% and 99.9\%. Note it means \emph{non-forest land}, not
young forest: those same zones had the OLDEST stands in the study area (median time
since fire 777 and 1067 years), so a young-fuel explanation is ruled out -- high
stand age is a consequence of under-burning, not its cause.
\item \code{cohortGap} = \code{pctTreedLCC - pctCohortsInit} -- treed land that never received
cohorts when the run was initialised. This separates "the zone genuinely is not
forest" from "the zone is forest that was never populated", which the other columns
cannot tell apart, and which imply completely different fixes. On
WesternAlbertaUpland the gap was 0.1-2.1 points in every zone that met its target
and 10.7-33.1 points in every zone that did not; cohort \emph{loss} over 1000 years was
at most 13.4\% anywhere and ~0\% in the two worst zones, so the shortfall is an
initialisation property, not regeneration failure.
}
}

\seealso{
\code{\link[=landmine_fri_verdict]{landmine_fri_verdict()}}, \code{\link[=landmine_plot_fri_zones]{landmine_plot_fri_zones()}}
}
