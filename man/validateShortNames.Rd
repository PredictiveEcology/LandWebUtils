% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shortNames.R
\name{validateShortNames}
\alias{validateShortNames}
\title{Validate a set of curated short names}
\usage{
validateShortNames(
  x,
  what = "NAME_SHORT",
  max_len = .maxShortNameLength,
  by = NULL
)
}
\arguments{
\item{x}{character vector of candidate short names.}

\item{what}{character used in error messages to name the table being validated.}

\item{max_len}{integer maximum permitted length (default \code{16}).}

\item{by}{optional grouping vector the same length as \code{x}; uniqueness is then required
only within each group.}
}
\value{
\code{x}, invisibly, if every check passes; otherwise an error.
}
\description{
Enforces the invariants the output keying depends on: every short name is present,
filesystem-safe (unchanged by \code{.slug()}, so \code{refCode} round-trips), no longer than
\code{max_len}, and unique --- globally, or within each level of \code{by} (used for the per-tenure
landbase codes, which need only be unique inside their tenure).
}
