% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shortNames.R
\name{tenureShortNames}
\alias{tenureShortNames}
\title{Curated short names for the LandWeb tenures}
\usage{
tenureShortNames()
}
\value{
A \code{tibble} with columns \code{fma_name} (the coalesced v10 identity) and \code{NAME_SHORT}.
}
\description{
One row per member tenure of the v10 combined boundary layer
(\code{FMAs_LandWebltfc_map_v10_LCC1}), mapping its \strong{coalesced identity} --- the same
\code{FMA_NAME}/\code{FMU_NAME}/\code{TSA_NUMB_1}/\code{FOREST_NAM}/\code{FML_NAME}/\code{Name} coalesce that
\code{\link[=build_studyarea_crosswalk]{build_studyarea_crosswalk()}} uses as \code{fma_name} --- to a curated \code{NAME_SHORT}.
}
\details{
\code{NAME_SHORT} is the partner-facing label: it names the tenure in every crossed
reporting unit ("\verb{<NAME_SHORT> <sub-region>}"), in the figure titles and facet strips,
and (via \code{.slug()}) in the \code{refCode} that keys the parquet aggregates and figure files.
Codes are derived from the \strong{v10 identity}, not from the legacy v2 study-area registry
(\link{LandWebStudyAreas}), because tenures change hands and the v10 layer is the authority.

Codes are hand-curated rather than generated so that adding a reporting layer, or a new
member to the boundary layer, can never silently rename an existing unit's outputs.
Adding a member to the v10 layer therefore requires adding its code here --- an
uncurated member is an error, not a silent fallback (see \code{\link[=build_studyarea_crosswalk]{build_studyarea_crosswalk()}}).
}
\seealso{
\code{\link[=validateShortNames]{validateShortNames()}}, \code{\link[=build_studyarea_crosswalk]{build_studyarea_crosswalk()}}
}
