% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-calibration.R
\name{landmine_optim_fireSizes}
\alias{landmine_optim_fireSizes}
\title{Fire sizes for calibration, in pixels, from areas in hectares}
\usage{
landmine_optim_fireSizes(
  pixelSize,
  areaHa = c(50, 200, 600, 1500, 3000),
  shapeRangeHa = c(10, 3000)
)
}
\arguments{
\item{pixelSize}{Pixel resolution, in metres.}

\item{areaHa}{Fire sizes, in hectares.}

\item{shapeRangeHa}{Range over which the shape target is supported; a warning is issued for
areas outside it, since the \code{log10(AREA)^4} term extrapolates sharply.}
}
\value{
Integer vector of fire sizes in pixels, with \code{areaHa} and \code{haPerPixel} attributes.
}
\description{
The calibration targets are defined in \strong{hectares} (Andison fitted the shape relationship
over 10--3,000 ha), but the fire model takes sizes in \strong{pixels}. Specifying pixel counts
directly therefore means different physical fires at every resolution: \code{c(10, ..., 1e5)}
pixels is 10--100,000 ha at 100 m but 57.6--576,000 ha at 240 m. That also defeats the point
of calibrating at several pixel sizes, which is to check the fitted parameters transfer
across resolutions -- only meaningful if the fires compared are the same physical size.
}
\examples{
landmine_optim_fireSizes(240)                      ## v3 resolution
landmine_optim_fireSizes(100)                      ## the resolution originally calibrated at
}
