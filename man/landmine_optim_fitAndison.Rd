% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-calibration.R
\name{landmine_optim_fitAndison}
\alias{landmine_optim_fitAndison}
\title{Andison-constrained objective for the LandMine fire-spread calibration}
\usage{
landmine_optim_fitAndison(
  par,
  ros,
  centreCell,
  fireSizes = 10^(2:5),
  shapeIntercept = 1.77,
  shapeSlope = 0.041,
  minIslandSizeHa = 2,
  islandRangeHa = c(100, Inf),
  replicates = 1L,
  wShape = 1,
  wSize = 1,
  wIsland = NULL,
  crnSeed = NULL
)
}
\arguments{
\item{par}{Numeric vector of length 7; see \code{\link[=landmine_optim_unpack]{landmine_optim_unpack()}}.}

\item{ros}{Path to the landscape raster (a path, not a \code{SpatRaster}: these are not
serializable to cluster workers).}

\item{centreCell}{Ignition cell.}

\item{fireSizes}{Target fire sizes, in pixels.}

\item{shapeIntercept, shapeSlope}{Coefficients for \code{\link[=landmine_optim_shapeTarget]{landmine_optim_shapeTarget()}}.}

\item{minIslandSizeHa}{Passed to \code{\link[=landmine_optim_islands]{landmine_optim_islands()}}.}

\item{islandRangeHa}{Length-2 range of fire areas over which the island term is applied.
Outside it the term is dropped, because the target is not supported there: Andison
"did not bother simulating many fires less than 100 hectares since disturbances below this
size are relatively constant and simple in shape, and generally do not produce remnant
islands", and the empirical island data (DeLong and Tanner 1995) covers 159--2,239 ha. A
small fire cannot satisfy the target at all -- a 9-pixel fire has no room for an interior
hole, so it scores the maximum penalty regardless of the parameters, which is noise rather
than signal.}

\item{replicates}{Number of independent evaluations to average per fire size. The island
statistic in particular is highly variable between draws, so \code{replicates > 1} buys
discrimination at linear cost.}

\item{wShape, wSize, wIsland}{Weights on the three components. Set one to 0 to drop it.
\code{wIsland} defaults to \code{\link[=landmine_optim_islandWeight]{landmine_optim_islandWeight()}} for the landscape's own resolution,
because a coarse grid cannot form the small islands that carry most of Andison's island
area. Pass an explicit value to override.}

\item{crnSeed}{Optional integer. If given, the RNG is seeded as \code{crnSeed + i} before the
\code{i}th fire, so every parameter vector is evaluated against the \strong{same} random
draws (common random numbers). This is variance reduction, not determinism:
it makes comparisons between parameter vectors far less noisy.}
}
\value{
A single numeric objective value, with the per-fire components attached as the
\code{"components"} attribute for diagnostics.
}
\description{
A replacement for \code{\link[=landmine_optim_fitSN]{landmine_optim_fitSN()}} that fits against the validation statistics
Andison (1996) actually reported, rather than a single constant perimeter-to-area target.
}
\details{
\code{\link[=landmine_optim_fitSN]{landmine_optim_fitSN()}} penalises departure from one constant \code{desiredPerimeterArea} at
\emph{every} fire size. Andison's central result, though, is that shape complexity \strong{increases
with fire area} -- that is what the \code{log10(AREA)^4} term describes -- so the old objective
works against the relationship it exists to reproduce. Combined with a size penalty that
never fired, it reduced to a single statistic constraining seven parameters, which left the
fitted values unidentifiable: across 43 repeat fits, four of seven scattered over more than
90\% of their search range, and repeat solutions were statistically indistinguishable from one
another (and from some randomly drawn parameter vectors).

This objective sums three squared relative deviations per fire size:

\itemize{
\item \strong{shape} -- observed vs \code{\link[=landmine_optim_shapeTarget]{landmine_optim_shapeTarget()}};
\item \strong{size} -- relative shortfall against the target size, continuous rather than a step,
so the optimiser gets a direction instead of a cliff;
\item \strong{islands} -- percent of fire area in remnant islands vs
\code{\link[=landmine_optim_islandTarget]{landmine_optim_islandTarget()}}.
}
}
\seealso{
\code{\link[=landmine_optim_fitSN]{landmine_optim_fitSN()}} for the original objective, retained so historical fits can
be reproduced.
}
