% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-events.R
\name{landmine_reburn_ceiling}
\alias{landmine_reburn_ceiling}
\title{Summarize FRI zones still short of target at the reburn ceiling}
\usage{
landmine_reburn_ceiling(polysNeedMoreFires, year)
}
\arguments{
\item{polysNeedMoreFires}{a \code{data.table} with columns \code{polygonNumeric} (the FRI zone),
\code{N} (fires still needed) and \code{maxSize} (pixels still to burn).}

\item{year}{numeric, the simulation year.}
}
\value{
A \code{data.table} with columns \code{year}, \code{FRI}, \code{nFires}, \code{pixelsShort}, containing only
zones with at least one outstanding fire; zero rows when nothing is short.
}
\description{
Summarize FRI zones still short of target at the reburn ceiling
}
\details{
Emitted only when the reburn loop exits on the \code{maxReburns} ceiling rather than because
every fire reached its target -- the two are otherwise indistinguishable in a log, and
the second means the year's area-burned target was abandoned unmet.

\code{pixelsShort} means different things in the two reburn phases: in phase 1 \code{maxSize} is a
fire's \emph{full} target, in phase 2 it is the \emph{remaining shortfall}. The caller is
responsible for passing the table from the phase it means.
}
