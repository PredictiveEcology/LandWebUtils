% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shortNames.R
\name{refCodeFor}
\alias{refCodeFor}
\title{Output key (\code{refCode}) for one analysis kind and reporting layer}
\usage{
refCodeFor(kind, layer)
}
\arguments{
\item{kind}{character analysis kind (\code{"lm"}, \code{"pm"}, \code{"lw"}, \code{"sspm"}, ...).}

\item{layer}{character reporting-layer name (a \code{reportingPolygons} list name).}
}
\value{
a character \code{refCode}.
}
\description{
The filesystem key an analysis writes under: it names the parquet aggregate directory
(\verb{_aggregates/<refCode>/}) and the figure/CSV filenames. Built by slugging the layer's
name --- which is its curated \code{NAME_SHORT}, or \code{"<tenure> <NAME_SHORT>"} for a crossed
unit --- so the on-disk key is exactly the label shown in the figures, and two units can
only collide if their curated short names do (which \code{\link[=validateShortNames]{validateShortNames()}} forbids).
}
\details{
This replaces the former \code{abbreviate(layer, minlength = 8)} keying, which gave no
cross-unit uniqueness guarantee: 68 of the 399 crossed FMA x ANSR names abbreviated onto
a name already taken, silently overwriting each other's aggregates and figures.
}
