% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-events.R
\name{landmine_attach_identity}
\alias{landmine_attach_identity}
\title{Attach fire identity to a burn result}
\usage{
landmine_attach_identity(fa, idDT)
}
\arguments{
\item{fa}{the \code{clusterDT} from a burn -- needs \code{initialPixels}.}

\item{idDT}{a \code{data.table} with \code{initialPixels}, \code{fireID}, \code{attempt} and \code{targetSize}, one
row per fire ignited this round.}
}
\value{
\code{fa} with \code{fireID}, \code{attempt} and \code{targetSize} added.
}
\description{
Joins \code{fireID}/\code{attempt}/\code{targetSize} onto the cluster table \code{spread2()} returns, keyed on
the fire's start cell.
}
\details{
\code{spread2()} returns clusters in its own order, which is not the order start cells were
handed to it, so identity must be joined rather than assigned by position. Row order of
\code{fa} is preserved.

Every burned cluster must match an ignited fire. An unmatched one would carry \code{NA} identity
into \code{fireSizes}, silently corrupting per-fire attainment while leaving
\code{sum(size) == sum(maxSize)} intact -- so nothing downstream would catch it. It is an error.
}
\examples{
fa <- data.table::data.table(initialPixels = c(12L, 11L), size = c(10, 500))
idDT <- data.table::data.table(
  initialPixels = c(11L, 12L), fireID = c(7L, 3L),
  attempt = c(1L, 2L), targetSize = c(500, 40)
)
landmine_attach_identity(fa, idDT)
}
