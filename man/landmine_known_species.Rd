% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-ros.R
\name{landmine_known_species}
\alias{landmine_known_species}
\title{Leading-species to fuel-type lookup used by LandMine}
\usage{
landmine_known_species()
}
\value{
A named character vector; names are species codes, values are fuel types.
}
\description{
The mapping from \code{LandWeb}-column species codes to the four LandMine fuel types
(\code{spruce}, \code{pine}, \code{decid}, \code{softwood}) that \code{\link[=landmine_fire_ros]{landmine_fire_ros()}} joins the
\code{ROSTable} on.
}
\details{
The LandMine module previously defined this inline in \code{init()} and used it in two
places, one of them inside \code{fireROS()}. Both now call this, so the fuel-type
assignment cannot drift between them.

Note the keys are \code{LandWeb}-column codes, \strong{not} \code{LandR}-column codes.
}
\examples{
landmine_known_species()[["Pice_mar"]] ## "spruce"
}
