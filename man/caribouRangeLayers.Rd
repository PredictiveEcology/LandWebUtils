% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caribou.R
\name{caribouRangeLayers}
\alias{caribouRangeLayers}
\title{Jurisdictional caribou range source layers}
\usage{
caribouRangeLayers()
}
\value{
A \code{tibble} with columns \code{juris}, \code{key}, \code{source}, \code{id}, \code{labelCols},
\code{statusCols} and \code{extirpated}.
}
\description{
The six provincial/territorial sources assembled into LandWeb's caribou reporting
layer by \code{\link[=buildCaribouRanges]{buildCaribouRanges()}}. LandWeb reports to \strong{jurisdictional} partners, who
each want their own management-unit names, so these are authoritative for reporting
rather than any national layer.
}
\details{
This replaces two earlier approaches. Julie Duval's pre-combined v10 layer
(\code{CaribouRange_Regional_ltfc_v10_LCC1}) was assembled by hand, and the combine had
\strong{dropped the Northwest Territories ranges} --- 0 of its 74 features intersect either
NWT tenure --- while leaving range names scattered across five per-jurisdiction
columns. A GNWT-only supplement then patched the NWT hole. Assembling from the
sources here removes both problems and the whole class of defect they came from: a bad
merge becomes a one-row fix instead of a re-delivery.

\code{source} uses the \code{\link[=reportingPolygonLayers]{reportingPolygonLayers()}} transports (\code{"drive"}, \code{"url"},
\code{"geojson"}). \code{labelCols} is a comma-separated priority list coalesced left to right,
as there. \code{statusCols} names the candidate column(s) carrying herd status and
\code{extirpated} the regular expression matching values to \strong{exclude}; both \code{NA} where a
source ships no status field.
}
\section{Why the ECCC national layer is not here}{

Measured against the assembled layer, ECCC adds no coverage: with designatable-unit
scope matched (boreal + mountain), the two footprints agree to \strong{0.7\%} --- the
assembly divides the same ground into 74 units where the federal layer has 52. The
difference is granularity and naming, not extent. ECCC's Southern Mountain units are
aggregate Local Population Units that \emph{merge} the finer provincial herds, and for
ranges wholly inside one province the provincial polygon is geometrically identical to
ECCC's (AB IoU = 1.0000 for 9 of 12 boreal ranges). It is retained as a documented
comparison in \code{scripts/make_caribou_reference.R}, not as a reporting layer.
}

\section{Per-source notes}{

\itemize{
\item \strong{AB} --- labelled on \code{SUBUNIT} before \code{LOCALRANGE}. v2 reported AB's mountain
contribution at subunit granularity: \verb{A La Peche}, \code{Narraway} and
\verb{Redrock-Prairie Creek} were three separate v2 units but are all subunits of the one
\code{LOCALRANGE} \code{"West Central"}, so labelling on \code{LOCALRANGE} alone would collapse
them. NB \code{Bischto} is Alberta's own misspelling of \code{Bistcho}; normalised by
\code{\link[=.caribouNameFixes]{.caribouNameFixes()}}.
\item \strong{BC} --- fetched live from the BC Data Catalogue WFS rather than a cached export.
The WFS release is \strong{newer and materially different} from the v2-era shapefile: 55
features against 72, and it flags \strong{5} extirpated herds where the old export flagged
\enumerate{
\item NB the WFS uses unabbreviated field names (\code{HERD_STATUS}, \code{RISK_STATUS}) while a
shapefile export truncates them to 10 characters (\code{HERD_STAT}), hence the candidate
list in \code{statusCols}.
}
\item \strong{SK} --- \code{CONUNIT} first, falling back to \code{RNGEUNIT}: SK1 carries the literal
\code{"Not Applicable"} in \code{CONUNIT}, which \code{\link[=buildReportingPolygons]{buildReportingPolygons()}}'s placeholder
handling treats as missing. Same footprint as ECCC (IoU 0.9983), subdivided finer.
\item \strong{MB} --- by-request from the Government of Manitoba, so Drive-hosted; there is no
public endpoint. Provided in \strong{2018} and \strong{current} --- Manitoba has published nothing
since, so this is the authoritative version, not a stale copy (the \verb{_2015} in the
filename is the delineation year, not the delivery). It is also \strong{newer than any ECCC
boundary set}, and 11.9\% of it falls outside every ECCC range, so it is not
substitutable.
\item \strong{NWT} --- GNWT MapServer layer 97, which subdivides NT1 into 6 named planning
regions the way \code{LOCALRANGE}/\code{HERD_NAME} subdivide AB/BC. Labelled on \code{REGION}, not
\code{NAME}: both exist, but \code{NAME} carries diacritics and apostrophes (\code{Sahtú},
\verb{Wek'èezhìi}, \verb{Gwich'in}) while \code{REGION} is ASCII-safe, and this label is slugged into
the \code{refCode} that keys parquet aggregates and figure filenames.
\item \strong{ON} --- the LIO direct download. Required, and easy to overlook: \code{RANGE_NAME} is
populated for exactly the ON and MB features of the old combined layer, so omitting
Ontario silently loses every ON tenure x Caribou reporting unit.
}
}

\seealso{
\code{\link[=buildCaribouRanges]{buildCaribouRanges()}}, \code{\link[=reportingPolygonLayers]{reportingPolygonLayers()}}
}
