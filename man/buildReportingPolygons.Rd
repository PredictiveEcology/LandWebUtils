% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reportingPolygons.R
\name{buildReportingPolygons}
\alias{buildReportingPolygons}
\title{Build the reporting-polygons named list}
\usage{
buildReportingPolygons(
  studyArea,
  destinationPath,
  targetCRS = LandWebCRS,
  layers = reportingPolygonLayers()
)
}
\arguments{
\item{studyArea}{A \code{SpatVector} (or source readable by \code{\link[terra:vect]{terra::vect()}}).}

\item{destinationPath}{Directory for downloads/extraction.}

\item{targetCRS}{Target CRS (default \link{LandWebCRS}).}

\item{layers}{Candidate-layer table (default \code{\link[=reportingPolygonLayers]{reportingPolygonLayers()}}).}
}
\value{
A named \code{list} of \code{SpatVector}s, one per intersecting layer.
}
\description{
Fetch each candidate reporting layer (\code{\link[=reportingPolygonLayers]{reportingPolygonLayers()}}), clip it to
\code{studyArea} with \code{\link[spatialutils:prep_vector]{spatialutils::prep_vector()}}, and keep only those that
intersect -- a layer that does not overlap the study area is dropped
(\code{nrow == 0}), which replaces the former per-study-area hardcoding. Downloads
use the idempotent \code{workflowtools} \verb{*_once} helpers (bypassing
\code{reproducible::prepInputs}). The result is a named list keyed by layer name,
suitable as the \code{reportingPolygons} input to \code{NRV_summary}; each layer's
\code{labelCol} is copied to a common \code{Name} column. The \code{"CC SAM"}/\code{"CC TSF"}
(current condition) and \code{"ecoregionLayer"} entries that \code{NRV_summary} also
expects come from the simulation, not from here, and are merged in separately.
}
