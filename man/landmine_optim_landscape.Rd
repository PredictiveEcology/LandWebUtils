% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-calibration.R
\name{landmine_optim_landscape}
\alias{landmine_optim_landscape}
\title{Build the synthetic landscape used for LandMine calibration}
\usage{
landmine_optim_landscape(
  pixelSize,
  n = 1000L,
  file = tempfile(fileext = ".tif")
)
}
\arguments{
\item{pixelSize}{Pixel resolution, in map units (m).}

\item{n}{Number of pixels per side. Default 1000, i.e. a 1e6-cell landscape.}

\item{file}{Optional path to write the raster to. The objective function takes the
landscape as a \emph{file path}, because \code{SpatRaster} objects cannot be
serialized to cluster workers.}
}
\value{
A list with \code{file} (path), \code{ros} (the \code{SpatRaster}), and \code{centreCell} (the cell
index at the centre of the landscape, used as the ignition point).
}
\description{
The calibration is deliberately run on a uniform, fully-flammable square rather than a
real study area, so that the fitted shape statistics are not confounded by a particular
landscape's fuel pattern. It is run at several pixel sizes to confirm the parameters
transfer across resolutions.
}
\examples{
\donttest{
lscape <- landmine_optim_landscape(pixelSize = 240, n = 100)
terra::ncell(lscape$ros)
}
}
