% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/current-conditions.R
\name{cc_age_pct_missing}
\alias{cc_age_pct_missing}
\title{Percent of forest with no current-condition stand age}
\usage{
cc_age_pct_missing(
  age,
  studyArea,
  landcover,
  forestClasses = lcc2020_classes()$forest
)
}
\arguments{
\item{age}{\code{SpatRaster} of stand age, e.g. from \code{\link[=cc_age_composite]{cc_age_composite()}}.}

\item{studyArea}{\code{SpatVector} or \code{sf} polygon(s).}

\item{landcover}{\code{SpatRaster} of land cover, in any CRS and at any resolution; it is cropped
and reprojected onto \code{age}'s grid with \code{method = "near"}.}

\item{forestClasses}{the \code{landcover} codes that count as forest. Defaults to the LCC 2020 forest
classes from \code{\link[=lcc2020_classes]{lcc2020_classes()}}.}
}
\value{
A single number, the percent (0-100) of forest cells with no age, with attributes
\code{nForest} and \code{nAged} giving the counts behind it.
}
\description{
The completeness check for a current-condition age raster: of the forest inside the study area,
what share has no age? Promoted from \code{LandWeb_preamble}'s \code{ccAgeMaxMissing} gate.
}
\details{
The denominator is the part of the study area that is forest. It took three attempts to get
there, and each wrong version is pinned by a test:
\enumerate{
\item \strong{Not the raster.} Cropping with \code{mask = TRUE} sets every cell outside the polygon to \code{NA},
indistinguishable from a cell that genuinely lacks an age, so \code{mean(is.na(age))} measures the
shape of the bounding box. LandWeb's study-area groups fill only 30-44\% of theirs:
WesternAlbertaUpland read 71.4\% missing against a true 3.9\%.
\item \strong{Not the whole study area.} Stand age is only defined for forest, and a treed-only source
such as NTEMS leaves water and wetland empty by design, so the polygon-wide figure mostly
measures lakes. ChurchillRiverUpland read 39.4\% missing after filling; 76\% of that was water,
barren or wetland.
\item \strong{Not on a coarser grid.} Aggregating with \code{average} fills a coarse cell from any one of its
children, so a cell only counts as missing when \emph{all} of them are. At 240 m that loosened the
figure 3-20x and let an unfilled study area pass.
}

Counting uses \code{terra::global()}, which works in chunks, so no full-resolution vector is held in
memory.
}
