% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-events.R
\name{landmine_fri_summary}
\alias{landmine_fri_summary}
\title{Summarize achieved fire return interval by FRI zone}
\usage{
landmine_fri_summary(
  lthfc,
  flammableMap,
  meanAnnualCumulBurnMap,
  studyAreaName
)
}
\arguments{
\item{lthfc}{\code{SpatRaster} of target fire return intervals (the "LTHFC").}

\item{flammableMap}{\code{SpatRaster}; pixels that are \code{NA} or \code{0} are non-flammable
and are excluded from both the numerator and the denominator.}

\item{meanAnnualCumulBurnMap}{\code{SpatRaster} of mean annual burn counts per pixel.}

\item{studyAreaName}{character, recorded in the returned table.}
}
\value{
A \code{data.table} with columns \code{studyArea}, \code{LTHFC}, \code{FRI}, sorted by \code{LTHFC}.
}
\description{
For each distinct value of \code{lthfc} (the target fire return interval, held as a
raster of per-pixel FRI values), computes the achieved FRI as the reciprocal of
the mean annual burn rate over that zone's \strong{flammable} pixels.
}
\details{
This was duplicated verbatim in the LandMine module's single- and multi-mode
summary functions, so the two could silently diverge.

Two edge cases are \strong{preserved as-is} rather than "fixed", because changing them
would change published outputs:
\itemize{
\item a zone in which nothing burned yields \code{1/0} = \code{Inf}, not \code{NA} and not a large
finite number;
\item the burn total is a plain \code{sum()} with no \code{na.rm}, so an \code{NA} burn pixel inside
a zone makes that whole zone's achieved FRI \code{NA}. Masking all three rasters by
\code{flammableMap} is what normally prevents this -- an implicit contract that the
\code{NA} masks of \code{lthfc} and \code{meanAnnualCumulBurnMap} agree. Both behaviours are
covered by tests so a future change to either is deliberate.
}
}
