% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landweb-species.R
\name{landweb_require_species}
\alias{landweb_require_species}
\title{Stop when a species input is empty}
\usage{
landweb_require_species(x, name)
}
\arguments{
\item{x}{the input to check: a \code{data.frame} (e.g. \code{sppEquiv}, \code{cohortData}),
a \code{SpatRaster} (e.g. \code{speciesLayers}), or a named list holding one of those
as element \code{name} (e.g. the result of \code{SpaDES.targets::sim_objects()}).}

\item{name}{character; the object's name, used to find it in a list and in
the error message.}
}
\value{
\code{x}, unchanged.
}
\description{
Checks that a species input handed between LandWeb pipeline stages is not
empty, and returns it unchanged so the check can wrap the reference inline.
}
\details{
The upstream \verb{Biomass_*} modules and \code{LandR} treat a study area with no tree
species as valid: an empty species table, \code{NULL} species layers or an empty
\code{cohortData} make them skip their work and finish without error. For
LandWeb every study area is forested, so an empty species input can only
mean a broken species mapping, and a run would otherwise "succeed" with no
vegetation dynamics. This turns that into an error at the stage boundary.
}
\examples{
landweb_require_species(data.frame(species = "Pice_mar"), "sppEquiv")
}
