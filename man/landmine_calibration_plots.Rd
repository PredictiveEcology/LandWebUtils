% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-calibration-plot.R
\name{landmine_calibration_plots}
\alias{landmine_calibration_plots}
\alias{landmine_plot_calibShape}
\alias{landmine_plot_calibIslands}
\alias{landmine_plot_calibDiscrimination}
\alias{landmine_plot_calibParams}
\title{LandMine calibration diagnostic plots}
\usage{
landmine_plot_calibShape(components, shapeRangeHa = c(10, 3000))

landmine_plot_calibIslands(components)

landmine_plot_calibDiscrimination(scores)

landmine_plot_calibParams(params, lower, upper)
}
\arguments{
\item{components}{The \code{"components"} attribute of a \code{\link[=landmine_optim_fitAndison]{landmine_optim_fitAndison()}} result: one
row per fire size (and replicate) with \code{areaHa}, \code{shapeObs}, \code{shapeExp},
\code{pctIslandsObs}, \code{pctIslandsExp}.}

\item{shapeRangeHa}{Range Andison fitted the shape relationship over; shaded on the plot,
because the \code{log10(AREA)^4} term extrapolates sharply outside it.}

\item{scores}{A \code{data.frame} with \code{group} (e.g. \code{"fitted"} / \code{"random"}) and \code{objective}.}

\item{params}{A matrix or \code{data.frame} of fitted parameter sets, one row per calibration run
(as returned by \code{\link[=landmine_optim_params_read]{landmine_optim_params_read()}}).}

\item{lower, upper}{Search bounds used for the fit, in the same parameter order.}
}
\value{
a \code{ggplot} object.
}
\description{
Figures for reporting what a fire-spread calibration achieved, and whether the objective can
actually tell good parameters from bad. They take plain data frames so the same functions
serve the module vignette, the project reports, and ad-hoc checks.
}
\details{
\itemize{
\item \code{landmine_plot_calibShape()} -- simulated shape index against Andison's fitted curve;
\item \code{landmine_plot_calibIslands()} -- simulated remnant-island fraction against Andison Table 3.5;
\item \code{landmine_plot_calibDiscrimination()} -- objective values for fitted vs random parameters;
\item \code{landmine_plot_calibParams()} -- spread of repeat fits within the search bounds.
}
}
