% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-calibration.R
\name{landmine_optim_unpack}
\alias{landmine_optim_unpack}
\alias{landmine_optim_params_read}
\alias{landmine_optim_params_append}
\title{Unpack LandMine fire-spread calibration parameters}
\usage{
landmine_optim_unpack(par)

landmine_optim_params_read(file, rowID = NULL)

landmine_optim_params_append(
  file,
  par,
  pixelSize,
  date = format(Sys.time(), "\%Y-\%m-\%d")
)
}
\arguments{
\item{par}{Numeric vector of length 7 to append (see \code{landmine_optim_unpack()}).}

\item{file}{Path to the calibration CSV.}

\item{rowID}{Optional row number to extract. If \code{NULL} (default), all rows are returned.}

\item{pixelSize}{Pixel resolution (m) the calibration was run at.}

\item{date}{Date of the run, as a string. Defaults to today.}
}
\value{
A named list with elements \code{spawnNewActive} (length 4), \code{sizeCutoffs}
(length 2), and \code{spreadProb} (length 1), all on the natural scale.

\code{landmine_optim_params_read()} returns a named numeric vector of length 7 when
\code{rowID} is given, otherwise a matrix with one row per calibration run.

\code{landmine_optim_params_append()} invisibly returns the full table as written.
}
\description{
The DEoptim calibration fits seven parameters on a transformed scale: the four
\code{spawnNewActive} probabilities and the two \code{sizeCutoffs} are fitted as base-10
logarithms, while \code{spreadProb} is fitted directly. This function is the single
definition of that convention -- it was previously repeated at eight call sites.

The calibration results live in a CSV (\code{LandMine_DEoptim_params.csv}) whose schema is
\code{date}, \code{pixelSize}, then \code{par1}..\code{par7}. These accessors are the single definition of
that schema; previously it was written in one place and read in two others, and the two
writers disagreed (one appended, one overwrote).
}
\examples{
landmine_optim_unpack(c(-0.22, -1.55, -0.65, -1.48, 3.21, 4.72, 0.856))
}
