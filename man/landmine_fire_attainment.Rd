% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmine-events.R
\name{landmine_fire_attainment}
\alias{landmine_fire_attainment}
\title{Per-fire target attainment from a LandMine \code{fireSizes} table}
\usage{
landmine_fire_attainment(fireSizes, by = "fireID")
}
\arguments{
\item{fireSizes}{a \code{data.table} (or coercible) with \code{fireID}, \code{attempt}, \code{targetSize},
\code{size} and \code{maxSize} -- i.e. \code{sim$fireSizes} from LandMine, row-bound across years.}

\item{by}{character; the columns that identify one fire. \code{fireID} is issued per burn YEAR,
so it is unique only within a year: a table spanning years needs \code{c("year", "fireID")},
and one spanning replicates \code{c("rep", "year", "fireID")}. Grouping too coarsely merges
unrelated fires and reports a target no single fire ever had.}
}
\value{
A \code{data.table} keyed by \code{fireID} with \code{burned} (pixels), \code{target} (pixels),
\code{attempts} and \code{reached}.
}
\description{
Collapses the per-burn rows to one row per fire and reports whether that fire ever
reached the target it was ignited for.
}
\details{
\code{size == maxSize} holds for \strong{every} row by construction, because a fire that stalls is
either discarded and retried at full target, or has its \code{maxSize} rewritten to the area
that did burn while the shortfall is re-issued as new fires. So \code{maxSize} cannot express a
shortfall, and attainment is only meaningful per \code{fireID} against \code{targetSize}: a fire is
short only when it exhausted \code{maxReburns[2]} and was abandoned.

\code{attempts} is the highest attempt index reached, so it answers "did this fire reach its
target on the first try, or the fourth?".
}
\examples{
fs <- data.table::data.table(
  fireID = c(1L, 2L, 2L), attempt = c(1L, 3L, 4L),
  targetSize = c(100, 200, 200), size = c(100, 60, 90), maxSize = c(100, 60, 90)
)
landmine_fire_attainment(fs)
}
