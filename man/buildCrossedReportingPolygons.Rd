% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reportingPolygonCrossings.R
\name{buildCrossedReportingPolygons}
\alias{buildCrossedReportingPolygons}
\title{Build the tenure x sub-region crossed reporting polygons}
\usage{
buildCrossedReportingPolygons(
  polys,
  landbases = list(),
  layers = reportingPolygonLayers(),
  members = NULL,
  min_area_km2 = 1
)
}
\arguments{
\item{polys}{Named list of study-area-clipped reporting layers, keyed by \code{NAME_SHORT}
(the output of \code{\link[=buildReportingPolygons]{buildReportingPolygons()}}); must contain the tenure layer.}

\item{landbases}{Named list of per-tenure landbase coverages keyed
\code{"<tenure> <NAME_SHORT>"} (the output of \code{\link[=buildLandbasePolygons]{buildLandbasePolygons()}}); may be empty.}

\item{layers}{Candidate-layer table (default \code{\link[=reportingPolygonLayers]{reportingPolygonLayers()}}), supplying
\code{isTenure} and \code{cross}.}

\item{members}{optional character vector of the study area's \strong{member} tenure short
names (a study-area group's \code{name_short}s from \code{\link[=studyAreaCrosswalk]{studyAreaCrosswalk()}}). When supplied,
only members are crossed. Tenure polygons overlap in the v10 layer, so clipping to a
group boundary also catches neighbouring tenures that belong to a \emph{different} group ---
and since the crosswalk assigns each tenure to exactly one group, crossing them here
would report a fragment of a tenure that is reported in full elsewhere. \code{NULL} (the
default) crosses every tenure present.}

\item{min_area_km2}{numeric; tenures contributing less than this to the study area are
not crossed (default \code{1}). A neighbouring tenure that merely grazes the study-area
boundary would otherwise mint a full set of reporting units over a handful of pixels.}
}
\value{
A named \code{list} of crossed layers (\code{SpatVector}s), ready to be appended to the
\code{reportingPolygons} input to \code{NRV_summary}. The tenure-crossed landbases are included
here, so the raw \code{landbases} list should \strong{not} also be appended.
}
\description{
Restores the v2 per-tenure reporting units on top of v3's study-area-clipped layers.
Each tenure in the tenure layer (dissolved to one geometry per tenure, see
\code{\link[=reportingPolygonLayers]{reportingPolygonLayers()}}\verb{$isTenure}) is crossed --- via \code{\link[=joinReportingPolygons]{joinReportingPolygons()}} ---
with:
}
\details{
\enumerate{
\item every sub-region layer marked \code{cross} in \code{layers} (Caribou ranges, Alberta Natural
Subregions, BC biogeoclimatic zones, NWT/national ecoregions, national ecozones);
\item its own active/passive landbase coverage(s), if any (\code{\link[=buildLandbasePolygons]{buildLandbasePolygons()}});
\item and, where a tenure has both, the landbase \strong{and} Caribou crossing together --- the
triple (\verb{tenure x landbase x Caribou}) v2 produced for the West Fraser CLS blocks.
}

Products are named \code{"<tenure> <layer NAME_SHORT>"} (e.g. \code{"SprayLake ANSR"},
\code{"Tolko_Vand_WF_SL LB_S17 Caribou"}), so each is a distinct \code{reportingPolygons} entry
with its own \code{refCode}, parquet aggregate directory, and figure folder. Their features
are named \code{"<sub-region> <tenure>"} by the concatenation in \code{\link[=joinReportingPolygons]{joinReportingPolygons()}}.
Crossings that do not intersect are dropped.
}
\seealso{
\code{\link[=buildReportingPolygons]{buildReportingPolygons()}}, \code{\link[=buildLandbasePolygons]{buildLandbasePolygons()}}, \code{\link[=joinReportingPolygons]{joinReportingPolygons()}}
}
